# DVD Library Integration

A Home Assistant custom integration to manage your DVD collection.

**Install via HACS**: Add this repo as a custom repository (type: Integration), download, restart HA, then add *DVD Library* from *Devices & services*.

Use domain services like `dvd_library.add_item`, `dvd_library.update_item`, `dvd_library.set_box`, etc.
